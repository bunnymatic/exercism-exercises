exit
exot
continue
@rolls.any?{|pins| pins.nil? || !pins.between?(0,10)}
continue
@rolls
@rolls.any?{|pins| pins.nil? || !pins.between?(0,10)}
continue
@rolls.any?{|pins| pins.nil? || !pins.between?(0,10)}
continue
@rolls.any?{|pins| pins.nil? || !pins.between?(0,10)}
continue
@rolls.any?{|pins| pins.nil? || !pins.between?(0,10)}
continue
@rolls.any?{|pins| pins.nil? || !pins.between?(0,10)}
continue
@rolls.any?{|pins| pins.nil? || !pins.between?(0,10)}
@rolls
